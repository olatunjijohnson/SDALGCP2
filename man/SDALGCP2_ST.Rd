% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st.R
\name{SDALGCP2_ST}
\alias{SDALGCP2_ST}
\title{Fit a spatio-temporal SDA-LGCP model (Kronecker-free)}
\usage{
SDALGCP2_ST(
  formula,
  data,
  my_shp,
  times,
  delta,
  phi = NULL,
  kappa = 0.5,
  kappa_t = 0.5,
  method = 3L,
  weighted = FALSE,
  pop_shp = NULL,
  control.mcmc = NULL,
  reanchor = 0L,
  messages = FALSE
)
}
\arguments{
\item{formula}{model formula (with optional \code{offset(log(pop))}).}

\item{data}{data frame of \code{N*T} rows ordered by time then region (rows
\code{(t-1)*N + 1:N} are time \code{t}).}

\item{my_shp}{\code{sf} polygons for the \code{N} regions.}

\item{times}{numeric vector of length \code{T} of observation times.}

\item{delta}{candidate-point spacing.}

\item{phi}{spatial-scale grid (default from geometry).}

\item{kappa}{spatial Matern smoothness.}

\item{kappa_t}{temporal Matern smoothness.}

\item{method, weighted, pop_shp}{point-generation controls.}

\item{control.mcmc}{list from \code{\link{control_mcmc}}.}

\item{reanchor}{number of re-anchoring passes (re-simulate the latent field at
the current optimum and refit); improves the variance-parameter estimates.}

\item{messages}{logical; print progress.}
}
\value{
an object of class \code{c("SDALGCP2_ST","SDALGCP2")} with \code{phi_opt},
  \code{nu_opt}, coefficient table and covariance.
}
\description{
Separable space-time SDA-LGCP for aggregated counts observed over the same
\code{N} regions at \code{T} times. The spatial scale \code{phi} is profiled on
a grid; the temporal Matern range \code{nu} is estimated continuously. The
likelihood never forms the \eqn{(NT)\times(NT)} covariance.
}
