% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misalignment.R
\name{SDALGCP2_misaligned}
\alias{SDALGCP2_misaligned}
\title{Fit an SDA-LGCP with covariates measured on a different support}
\usage{
SDALGCP2_misaligned(
  formula,
  data,
  delta,
  covariates,
  phi = NULL,
  method = 3L,
  weighted = FALSE,
  pop_shp = NULL,
  berkson = TRUE,
  control.mcmc = NULL,
  max_iter = 10L,
  tol = 0.001,
  messages = FALSE
)
}
\arguments{
\item{formula}{model formula; the covariate names appear on the right-hand side.}

\item{data}{\code{sf} polygons holding the response and offset (one row/region).}

\item{delta}{candidate-point spacing.}

\item{covariates}{a named list; each element is an \code{sf} carrying a column
of the same name -- the covariate's observed values on its own support, either
\strong{points} (e.g. monitors; plain kriging) or \strong{polygons} (areal
averages on a different partition; aggregated areal kriging).}

\item{phi}{spatial-scale grid for the outcome model (default from geometry).}

\item{method, weighted, pop_shp}{point-generation controls.}

\item{berkson}{logical; include the Berkson uncertainty correction (default
\code{TRUE}). \code{FALSE} gives the naive kriged-mean plug-in.}

\item{control.mcmc}{list from \code{\link{control_mcmc}}.}

\item{max_iter, tol}{outer Gauss-Newton controls.}

\item{messages}{logical; print progress.}
}
\value{
an object of class \code{"SDALGCP2"} with \code{misaligned = TRUE}.
}
\description{
Covariates observed on a \emph{different support} from the outcome (e.g. air-
quality monitors at point locations) are kriged to the candidate points and
enter the model on the intensity scale with a Berkson correction that propagates
the prediction uncertainty (see \code{math/confounding-and-misalignment.pdf}).
}
\examples{
\donttest{
data(sdalgcp_data)
set.seed(1)
## a covariate z observed only at 40 scattered monitor points (a different support)
mon <- sf::st_as_sf(data.frame(x = runif(40, 0, 20), y = runif(40, 0, 20)),
                    coords = c("x", "y"))
mon$z <- scale(sf::st_coordinates(mon)[, 1])[, 1]
fit <- SDALGCP2_misaligned(cases ~ z + offset(log(pop)), sdalgcp_data, delta = 1.5,
                           covariates = list(z = mon),
                           control.mcmc = control_mcmc(n.sim = 2000, burnin = 500, thin = 5))
summary(fit)
}
}
\seealso{
\code{\link{SDALGCP2_raster}}, \code{\link{sdalgcp}}
}
