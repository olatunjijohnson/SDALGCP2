% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-sdalgcp.R
\name{plot.sdalgcp}
\alias{plot.sdalgcp}
\title{Map an sdalgcp fit}
\usage{
\method{plot}{sdalgcp}(
  x,
  what = c("relative_risk", "adjusted_rr", "relative_risk_se", "adjusted_rr_se",
    "exceedance"),
  type = c("discrete", "continuous"),
  time = NULL,
  threshold = 1,
  which = c("adjusted_rr", "relative_risk"),
  cellsize = NULL,
  sampler = c("mcmc", "laplace"),
  ...
)
}
\arguments{
\item{x}{an \code{"sdalgcp"} fit.}

\item{what}{one of \code{"relative_risk"} (relative risk, default),
\code{"adjusted_rr"} (covariate-adjusted relative risk),
\code{"relative_risk_se"}, \code{"adjusted_rr_se"} or \code{"exceedance"}.}

\item{type}{\code{"discrete"} (default) or \code{"continuous"} (spatial fits).}

\item{time}{for spatio-temporal fits, the time to map (default: first; use
\code{NULL} to facet all times).}

\item{threshold}{threshold for \code{what = "exceedance"}.}

\item{which}{for exceedance: \code{"adjusted_rr"} (default) or
\code{"relative_risk"}.}

\item{cellsize}{grid spacing for \code{type = "continuous"}.}

\item{sampler}{\code{"mcmc"} (default) or \code{"laplace"}.}

\item{...}{passed to the mapping layer.}
}
\value{
a \code{ggplot} object.
}
\description{
Predicts and maps a chosen quantity. Works for spatial fits (discrete or
continuous) and spatio-temporal fits (select a \code{time}).
}
\examples{
\donttest{
data(sdalgcp_data)
fit <- sdalgcp(cases ~ x1 + offset(log(pop)), data = sdalgcp_data,
               control = sdalgcp_control(n_sim = 2000, burnin = 500, thin = 5,
                                         reanchor = 0))
plot(fit)                              # relative-risk map (predicts internally)
plot(fit, what = "exceedance", threshold = 1.5)
}
}
