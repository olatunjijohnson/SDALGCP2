% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance.R
\name{precompute_corr_ref}
\alias{precompute_corr_ref}
\title{Reference (pure-R) aggregated correlation builder}
\usage{
precompute_corr_ref(points, phi, weighted = NULL)
}
\arguments{
\item{points}{a list of length \eqn{N}; each element holds \code{$xy} (an
\eqn{n_i \times 2} matrix of candidate-point coordinates) and, when
weighted, \code{$weight} (a length-\eqn{n_i} vector summing to 1). The
\code{"weighted"} and \code{"my_shp"} attributes produced by the
point-generation step are honoured and carried through.}

\item{phi}{numeric vector of spatial scale parameters.}

\item{weighted}{logical; if \code{NULL} (default) it is taken from
\code{attr(points, "weighted")}.}
}
\value{
the \eqn{N \times N \times} \code{length(phi)} correlation array.
}
\description{
Slow but dependency-light implementation kept for correctness testing and
benchmarking against \code{\link{precompute_corr}}. Computes the exponential
(\code{kappa = 0.5}) aggregated correlation only.
}
\keyword{internal}
