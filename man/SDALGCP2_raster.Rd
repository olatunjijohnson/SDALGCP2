% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{SDALGCP2_raster}
\alias{SDALGCP2_raster}
\title{Fit an SDA-LGCP with spatially continuous (raster) covariates}
\usage{
SDALGCP2_raster(
  formula,
  data,
  my_shp,
  delta,
  rasters,
  phi = NULL,
  method = 3L,
  weighted = FALSE,
  pop_shp = NULL,
  kappa = 0.5,
  tilt_spatial = FALSE,
  control.mcmc = NULL,
  max_iter = 10L,
  tol = 0.001,
  messages = FALSE
)
}
\arguments{
\item{formula}{model formula; right-hand-side names must match raster layer
names. The response and an \code{offset(log(pop))} come from \code{data}.}

\item{data}{data frame with the response and offset (one row per region).}

\item{my_shp}{\code{sf} polygons.}

\item{delta}{candidate-point spacing.}

\item{rasters}{a \code{terra::SpatRaster} (or object coercible by
\code{terra::rast}) whose layers are the spatially varying covariates named
in \code{formula}.}

\item{phi}{spatial-scale grid (default chosen from the geometry).}

\item{method, weighted, pop_shp}{point-generation controls (see \code{\link{sda_points}}).}

\item{kappa}{Matern smoothness for the spatial correlation.}

\item{tilt_spatial}{logical; if \code{FALSE} (default) the spatial correlation
uses the population weights and is precomputed once (covariates enter only
through the log-sum-exp offset). If \code{TRUE}, the correlation
\eqn{R^c(\beta)} is rebuilt each iteration from the intensity-tilted weights
\eqn{c_{ik}(\beta)} and a log-normal aggregation correction
\eqn{\tfrac12\sigma^2(1-R^c_{ii})} is added -- the fully tilted model (more
accurate, more costly).}

\item{control.mcmc}{list from \code{\link{control_mcmc}}.}

\item{max_iter, tol}{outer Gauss-Newton iteration controls.}

\item{messages}{logical; print progress.}
}
\value{
an object of class \code{"SDALGCP2"} (as \code{\link{mcml_fit}}) with
  extra fields \code{raster = TRUE} and \code{n_iter}.
}
\description{
Covariates supplied as rasters enter the model at the candidate-point level and
are aggregated on the intensity (exp) scale via a log-sum-exp offset
\eqn{b_i(\beta)=\log\sum_k w_{ik}\exp(z(x_{ik})^\top\beta)} -- the statistically
correct alternative to averaging the predictor over each polygon. Estimation is
a Gauss-Newton fixed point that reuses \code{\link{mcml_fit}} with the
intensity-tilted effective design.
}
\seealso{
\code{\link{SDALGCP2}}, \code{\link{mcml_fit}}
}
